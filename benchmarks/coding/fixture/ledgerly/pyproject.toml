[project]
name = "ledgerly"
version = "0.3.0"
description = "A small personal expense ledger with a CLI and CSV import"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ledgerly = "ledgerly.cli:main"
