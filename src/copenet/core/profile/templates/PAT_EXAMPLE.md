# Pat Example

This template area is generic, but CopeNet's default profile model is meant to support a personal operator workflow:

- recurring priorities
- goals
- tone preferences
- noise filters
- observed tendencies
- corrective guidance rules

Keep real private values in `~/.copenet/profile/`.
